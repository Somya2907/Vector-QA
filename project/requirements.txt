# ── LLM & Embeddings ──────────────────────────────────────────────────────────
anthropic>=0.30.0               # Claude API client
voyageai>=0.3.0                 # Voyage embedding models (recommended with Claude)
sentence-transformers>=3.0.0    # Local embedding models (all-MiniLM-L6-v2 default)
numpy>=1.26.0                   # Numerical arrays for embeddings

# ── Vector Store ──────────────────────────────────────────────────────────────
faiss-cpu>=1.8.0                # FAISS exact / approximate nearest-neighbour search
rank_bm25>=0.2.2                # BM25Okapi sparse retrieval

# ── Vector Store (optional backends) ─────────────────────────────────────────
# chromadb>=0.5.0               # Uncomment for ChromaDB backend
# pinecone-client>=3.0.0        # Uncomment for Pinecone backend
# weaviate-client>=4.0.0        # Uncomment for Weaviate backend

# ── UI ────────────────────────────────────────────────────────────────────────
streamlit>=1.35.0               # Web UI

# ── API Framework ─────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0

# ── Configuration & Environment ───────────────────────────────────────────────
python-dotenv>=1.0.0

# ── Logging ───────────────────────────────────────────────────────────────────
structlog>=24.1.0

# ── Document Parsing ──────────────────────────────────────────────────────────
pypdf>=4.2.0                    # PDF extraction
python-docx>=1.1.0              # DOCX extraction
beautifulsoup4>=4.12.0          # HTML extraction
boto3>=1.34.0                   # S3 document loading

# ── Text Splitting ────────────────────────────────────────────────────────────
tiktoken>=0.7.0                 # Token counting for chunking

# ── Evaluation ────────────────────────────────────────────────────────────────
ragas>=0.1.10                   # RAG evaluation metrics (faithfulness, correctness)

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.0
httpx>=0.27.0                   # Async HTTP client for FastAPI test client
